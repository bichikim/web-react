# Update Existing Hook Example

If modifying an existing hook:

1. Add or adjust a failing test for the target behavior.
2. Implement the minimal hook change.
3. Confirm existing tests still pass.

Prefer keeping the public return shape unless change is requested.
